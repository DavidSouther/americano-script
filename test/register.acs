var compiler = require('../lib/americano')

describe 'Can register with node', function()
    it 'has filled in block', function()
        var source = "if ((a or b) and (c is d)) 3\n"
        var expected = "if ( ((a || b) && (c === d)) 3 )\n"
        compiler.compile(source).should.equal(expected)
